echo ‘post-receive: Triggered..’
cd /summerfoodrocks-org/public/
echo ‘post-receive: git check out…’
git --work-tree=/apps/summerfoodrocks-org/public/sfr.git --git-dir=/apps/summerfoodrocks-org/sfr.git checkout master -f
echo ‘post-receive: npm install…’
npm install \
# && echo ‘post-receive: building…’ \
# && npm run build \
echo ‘post-receive: → done.’ \
&& (pm2 delete ‘sfrserver.js’ || true) \
&& NODE_ENV=production pm2 start npm --name ‘sfrserver.js’ -- start \
&& echo ‘post-receive: app started successfully with pm2